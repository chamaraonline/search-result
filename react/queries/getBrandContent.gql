query getBrandContent(
  $acronym: String
  $fields: [String]
  $where: String
  $schema: String
) {
  documents(
    acronym: $acronym
    fields: $fields
    where: $where
    schema: $schema
  ) {
    fields {
      key
      value
    }
  }
}
